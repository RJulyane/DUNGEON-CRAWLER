#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
int tam=9;
char cenario[10][10];
int x, y;
int x1, y1;
int i, j;
void cenarioatual{
	system("cls");
	for (i=0;i<tam;i++){
		for (j=0;j<tam;j++){
			printf("%c", cenario[i][j]);
		}
		printf("\n");
	}
}

int main(){
	for (i=0;i<tam;i++){
		for (j=0;j<tam;j++){
			if (i==0||i==tam){
				cenario[i][j]='*';
			}
			else if (j==0||j==tam){
				cenario[i][j]='*';
			}
			else{
				cenario[i][j]='  ';
			}
		}
	}
	cenarioatual();
	return 0;
}
